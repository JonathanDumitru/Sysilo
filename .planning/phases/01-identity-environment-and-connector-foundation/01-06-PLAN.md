---
phase: 01-identity-environment-and-connector-foundation
plan: "06"
type: execute
wave: 6
depends_on:
  - "01-05"
files_modified:
  - packages/frontend/web-app/src/services/api.ts
  - packages/frontend/web-app/src/services/connections.ts
  - packages/frontend/web-app/src/hooks/useConnections.ts
  - packages/frontend/web-app/src/config/env.ts
  - packages/frontend/web-app/.env.example
  - services/api-gateway/cmd/api-gateway/main.go
autonomous: true
gap_closure: true
requirements:
  - GOV-02
  - PLAT-01
user_setup:
  - service: frontend_env
    why: "Frontend must target API gateway by default so gateway RBAC is always enforced"
    env_vars:
      - name: VITE_API_BASE_URL
        source: "Set to gateway URL (example: http://localhost:8080)"
    local_dev:
      - "Remove/avoid direct integration-service URL defaults in frontend configs"
must_haves:
  truths:
    - "Frontend defaults to gateway API base URL, not integration-service direct URL"
    - "All connection operations from frontend traverse gateway RBAC checks"
    - "Direct-service bypass defaults are removed from app config and docs"
  artifacts:
    - path: "packages/frontend/web-app/src/services/api.ts"
      provides: "Single API client that targets gateway by default"
      contains: "VITE_API_BASE_URL"
    - path: "packages/frontend/web-app/src/config/env.ts"
      provides: "Runtime config validation for gateway-oriented API URL"
      min_lines: 15
    - path: "packages/frontend/web-app/.env.example"
      provides: "Developer guidance for required gateway API base setting"
      contains: "VITE_API_BASE_URL"
  key_links:
    - from: "packages/frontend/web-app/src/services/connections.ts"
      to: "packages/frontend/web-app/src/services/api.ts"
      via: "Connections service uses shared gateway API client"
      pattern: "apiClient|request"
    - from: "packages/frontend/web-app/src/services/api.ts"
      to: "services/api-gateway/cmd/api-gateway/main.go"
      via: "Configured base path maps to gateway route surface"
      pattern: "/api|/connections"
---

<objective>
Eliminate frontend RBAC bypass by forcing frontend API traffic through gateway as the default and documented path.

Purpose: close verification gap where web app could call integration service directly and skip gateway authorization controls.
Output: gateway-first frontend API configuration with validation and updated env guidance.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
@packages/frontend/web-app/src/services/api.ts
@packages/frontend/web-app/src/services/connections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set gateway as enforced frontend API default</name>
  <files>packages/frontend/web-app/src/services/api.ts, packages/frontend/web-app/src/config/env.ts, packages/frontend/web-app/.env.example</files>
  <action>Update API base resolution so default and recommended value points to API gateway. Add env validation that warns/fails for missing base URL in non-test environments. Remove hardcoded integration-service direct fallback.</action>
  <verify>pnpm --filter web-app test -- api-config</verify>
  <done>Frontend API client is gateway-first and no longer defaults to integration-service bypass URL.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure connections data layer uses shared gateway client only</name>
  <files>packages/frontend/web-app/src/services/connections.ts, packages/frontend/web-app/src/hooks/useConnections.ts</files>
  <action>Refactor connection service/hooks to use shared API client path exclusively. Remove any alternate/direct host logic and keep environment/team headers on all requests.</action>
  <verify>pnpm --filter web-app test -- connections-service-rbac-path</verify>
  <done>All frontend connection operations route through gateway client with required auth context headers.</done>
</task>

<task type="auto">
  <name>Task 3: Add route compatibility checks against gateway surface</name>
  <files>packages/frontend/web-app/src/services/api.ts, services/api-gateway/cmd/api-gateway/main.go</files>
  <action>Confirm frontend route paths match gateway route definitions. Add a light compatibility test or contract fixture to prevent drift between frontend service paths and gateway endpoints.</action>
  <verify>go test ./services/api-gateway/cmd/api-gateway -run "Connections|Routes" && pnpm --filter web-app test -- api-route-contract</verify>
  <done>Frontend and gateway route contracts align, preventing accidental reintroduction of bypass paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm --filter web-app test -- api-config connections-service-rbac-path api-route-contract
- [ ] Manual UI smoke: open Connections page and confirm network calls target gateway base URL
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend path enforces gateway RBAC by default with no direct-service bypass default

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-06-SUMMARY.md`
</output>
