---
phase: 01-identity-environment-and-connector-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - services/api-gateway/internal/authorization/environment_policy.go
  - services/api-gateway/internal/middleware/middleware.go
  - services/integration-service/src/middleware/mod.rs
  - services/integration-service/src/connections/api.rs
  - services/integration-service/src/storage/mod.rs
  - packages/frontend/web-app/src/components/EnvironmentSwitcher.tsx
  - packages/frontend/web-app/src/App.tsx
  - packages/frontend/web-app/src/pages/ConnectionsPage.tsx
autonomous: true
requirements:
  - GOV-02
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - "User role enforcement differs by environment for the same tenant user"
    - "Integration/connection visibility is constrained to selected environment"
    - "Production mutating actions require confirmation and reason before execution"
  artifacts:
    - path: "services/api-gateway/internal/authorization/environment_policy.go"
      provides: "Role x environment x action policy evaluation"
      contains: "prod"
    - path: "services/integration-service/src/middleware/mod.rs"
      provides: "Environment context validation on protected routes"
      contains: "x-environment"
    - path: "packages/frontend/web-app/src/components/EnvironmentSwitcher.tsx"
      provides: "Persistent environment selector and badge UX"
      min_lines: 30
  key_links:
    - from: "packages/frontend/web-app/src/components/EnvironmentSwitcher.tsx"
      to: "services/api-gateway/internal/middleware/middleware.go"
      via: "selected environment propagated on API calls"
      pattern: "x-environment"
    - from: "services/api-gateway/internal/middleware/middleware.go"
      to: "services/integration-service/src/middleware/mod.rs"
      via: "validated environment context forwarded to downstream service"
      pattern: "environment"
---

<objective>
Implement environment-bounded authorization and environment-specific visibility/controls across gateway, integration service, and frontend UX.

Purpose: Deliver GOV-02 and PLAT-01 guardrails so users can only act within approved team/environment boundaries.
Output: Environment-aware RBAC policy checks, resource scoping enforcement, and environment switcher with production safeguards.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-CONTEXT.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-RESEARCH.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-01-SUMMARY.md
@services/integration-service/src/middleware/mod.rs
@packages/frontend/web-app/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce environment-scoped RBAC policy model in gateway</name>
  <files>services/api-gateway/internal/authorization/environment_policy.go, services/api-gateway/internal/middleware/middleware.go</files>
  <action>Define canonical environments (dev/staging/prod), role bindings per environment, and a policy matrix for read/write/admin actions. Require environment context for protected routes and deny requests with missing or invalid environment.</action>
  <verify>go test ./services/api-gateway/internal/authorization ./services/api-gateway/internal/middleware -run "Environment|RBAC|Policy"</verify>
  <done>Gateway enforces environment-aware role checks for all protected API operations.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce environment scoping for integration-service connection APIs</name>
  <files>services/integration-service/src/middleware/mod.rs, services/integration-service/src/connections/api.rs, services/integration-service/src/storage/mod.rs</files>
  <action>Require validated environment context in integration-service middleware, persist/query connection records by environment, and reject cross-environment reads/writes even within same tenant.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml environment_scope -- --nocapture</verify>
  <done>Connection list/read/write paths are environment-bounded and cannot leak across dev/staging/prod.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend environment switcher and production mutation guardrail</name>
  <files>packages/frontend/web-app/src/components/EnvironmentSwitcher.tsx, packages/frontend/web-app/src/App.tsx, packages/frontend/web-app/src/pages/ConnectionsPage.tsx</files>
  <action>Add persistent global environment selector and badge in app shell, propagate selected environment on API requests, and require explicit confirmation plus mandatory reason field before production mutating operations proceed.</action>
  <verify>pnpm --filter web-app test -- environment-switcher production-guardrail</verify>
  <done>Users can switch environments globally, see active environment clearly, and cannot execute production mutations without confirmation and reason.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go test ./services/api-gateway/...
- [ ] cargo test --manifest-path services/integration-service/Cargo.toml
- [ ] pnpm --filter web-app test -- environment-switcher production-guardrail
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- GOV-02 and PLAT-01 enforcement is active end-to-end for environment scope and production guardrails

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-02-SUMMARY.md`
</output>
