---
phase: 01-identity-environment-and-connector-foundation
plan: "08"
type: execute
wave: 8
depends_on:
  - "01-04"
  - "01-05"
  - "01-06"
  - "01-07"
files_modified:
  - .planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
  - scripts/verify/phase1_gap_closure.sh
  - scripts/verify/check_go_toolchain.sh
  - services/integration-service/src/api/mod.rs
  - services/integration-service/src/playbooks/api.rs
  - package.json
autonomous: true
gap_closure: true
requirements:
  - GOV-03
  - GOV-02
  - PLAT-01
  - INVT-01
  - PLAT-02
user_setup:
  - service: local_toolchains
    why: "Verification currently blocked by missing Go and pre-existing Rust compile errors"
    env_vars: []
    dashboard_config:
      - task: "Install Go toolchain matching repo version policy"
        location: "Developer workstation"
        details: "Install and expose `go` in PATH before running gateway tests"
    local_dev:
      - "Run `scripts/verify/check_go_toolchain.sh` and follow install hint if missing"
must_haves:
  truths:
    - "Verification for Phase 1 gaps has an executable, repeatable script path"
    - "Go missing-toolchain condition is explicitly detected and reported with actionable guidance"
    - "Rust pre-existing compile blockers are either fixed or isolated so phase-targeted tests can run"
  artifacts:
    - path: "scripts/verify/phase1_gap_closure.sh"
      provides: "Single command to run gateway/integration/frontend gap-closure verification suite"
      contains: "cargo test"
    - path: "scripts/verify/check_go_toolchain.sh"
      provides: "Preflight toolchain check for go availability/version"
      contains: "go version"
    - path: ".planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md"
      provides: "Updated verification evidence and residual risk notes after rerun"
      contains: "verified_on"
  key_links:
    - from: "scripts/verify/phase1_gap_closure.sh"
      to: "services/api-gateway"
      via: "Runs gateway gap-closure tests when Go is available"
      pattern: "go test"
    - from: "scripts/verify/phase1_gap_closure.sh"
      to: "services/integration-service/src/api/mod.rs"
      via: "Includes rust compile/test path after resolving or isolating blockers"
      pattern: "cargo test"
---

<objective>
Deliver a practical verification/tooling closure path so Phase 1 gap-fix plans can be validated despite current toolchain and compile blockers.

Purpose: remove uncertainty from blocked test execution and produce trustworthy pass/fail evidence for gap closure completion.
Output: deterministic verification scripts, toolchain preflight checks, and updated verification report with concrete outcomes.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
@services/integration-service/src/api/mod.rs
@services/integration-service/src/playbooks/api.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verification preflight for missing Go toolchain</name>
  <files>scripts/verify/check_go_toolchain.sh, scripts/verify/phase1_gap_closure.sh</files>
  <action>Create a preflight script that validates Go availability and version policy before running gateway tests. Ensure output clearly distinguishes toolchain missing vs test failures.</action>
  <verify>bash scripts/verify/check_go_toolchain.sh</verify>
  <done>Go readiness is machine-checkable with clear remediation guidance when missing.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve or isolate pre-existing Rust compile blockers for target suites</name>
  <files>services/integration-service/src/api/mod.rs, services/integration-service/src/playbooks/api.rs, scripts/verify/phase1_gap_closure.sh</files>
  <action>Fix known `ApiError` mismatch compile issues where feasible within owned impact, or isolate non-phase modules from phase-targeted test selection so gap tests can execute reliably. Document any residual non-phase blocker explicitly.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml --no-fail-fast</verify>
  <done>Rust verification path for Phase 1 gap fixes is no longer blocked by previously known compile failures.</done>
</task>

<task type="auto">
  <name>Task 3: Build single-command gap-closure verification runner and refresh report</name>
  <files>scripts/verify/phase1_gap_closure.sh, .planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md, package.json</files>
  <action>Create one script command that runs gateway, integration-service, and frontend targeted verification in dependency-aware order, captures results, and updates verification report with concrete pass/fail evidence plus residual risks.</action>
  <verify>bash scripts/verify/phase1_gap_closure.sh</verify>
  <done>Team can rerun complete Phase 1 gap verification from a single command with reproducible outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bash scripts/verify/check_go_toolchain.sh
- [ ] bash scripts/verify/phase1_gap_closure.sh
- [ ] Verification report updated with command outputs, exact blocker status, and date-stamped evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass (or clearly report external blocker ownership)
- No errors or warnings introduced
- Phase 1 gap-fix verification has reproducible tooling and current evidence despite prior blocked-test conditions

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-08-SUMMARY.md`
</output>
