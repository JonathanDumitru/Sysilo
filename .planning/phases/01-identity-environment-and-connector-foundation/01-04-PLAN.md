---
phase: 01-identity-environment-and-connector-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-01"
files_modified:
  - services/api-gateway/cmd/api-gateway/main.go
  - services/api-gateway/internal/handlers/auth_sso.go
  - services/api-gateway/internal/handlers/auth_breakglass.go
  - services/api-gateway/internal/handlers/auth_session.go
  - services/api-gateway/internal/handlers/scim_users.go
  - services/api-gateway/internal/middleware/middleware.go
autonomous: true
gap_closure: true
requirements:
  - GOV-03
user_setup: []
must_haves:
  truths:
    - "OIDC SSO, break-glass, session refresh, and SCIM user lifecycle endpoints are registered in gateway router"
    - "Identity endpoints are protected with correct auth/authorization boundaries"
    - "SSO-to-SCIM lifecycle is reachable end-to-end through API gateway"
  artifacts:
    - path: "services/api-gateway/cmd/api-gateway/main.go"
      provides: "Route registration for auth/session/scim handlers"
      contains: "/auth/sso"
    - path: "services/api-gateway/internal/handlers/scim_users.go"
      provides: "SCIM user create/update/deactivate handlers reachable via router"
      contains: "Deactivate"
    - path: "services/api-gateway/internal/handlers/auth_session.go"
      provides: "Session refresh handler wired to route"
      contains: "RefreshSession"
  key_links:
    - from: "services/api-gateway/cmd/api-gateway/main.go"
      to: "services/api-gateway/internal/handlers/auth_sso.go"
      via: "Router binds HTTP endpoints to SSO handlers"
      pattern: "StartSSO|HandleSSOCallback"
    - from: "services/api-gateway/cmd/api-gateway/main.go"
      to: "services/api-gateway/internal/handlers/scim_users.go"
      via: "Router binds SCIM Users endpoints"
      pattern: "scim"
---

<objective>
Expose identity lifecycle features implemented in Plan 01-01 through API gateway routing, with correct middleware and access controls.

Purpose: close the end-to-end activation gap for GOV-03 by making implemented SSO/SCIM/session flows reachable in production topology.
Output: gateway route wiring for auth and SCIM handlers plus route-level auth guards.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-01-SUMMARY.md
@services/api-gateway/cmd/api-gateway/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register missing identity routes in gateway</name>
  <files>services/api-gateway/cmd/api-gateway/main.go</files>
  <action>Add route bindings for SSO start/callback, break-glass start/complete, session refresh, and SCIM Users endpoints. Keep REST path structure consistent with existing gateway route style.</action>
  <verify>go test ./services/api-gateway/cmd/api-gateway -run "Route|Auth|SCIM"</verify>
  <done>All required identity handlers are reachable through router with expected HTTP methods and paths.</done>
</task>

<task type="auto">
  <name>Task 2: Apply route-level middleware boundaries</name>
  <files>services/api-gateway/cmd/api-gateway/main.go, services/api-gateway/internal/middleware/middleware.go</files>
  <action>Ensure public-only routes remain limited to login callbacks where needed, while refresh/break-glass/SCIM routes require authenticated and authorized contexts. Enforce SCIM token validation and admin scope checks on SCIM paths.</action>
  <verify>go test ./services/api-gateway/internal/middleware ./services/api-gateway/internal/handlers -run "SCIM|Authz|Refresh|Breakglass"</verify>
  <done>Identity routes are exposed with no privilege broadening and expected access control semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration-level route tests for identity flow reachability</name>
  <files>services/api-gateway/cmd/api-gateway/main.go, services/api-gateway/internal/handlers/auth_sso.go, services/api-gateway/internal/handlers/scim_users.go</files>
  <action>Add/extend tests that verify router registration and handler invocation for SSO callback, session refresh, and SCIM deactivate. Assert deactivated users are denied in subsequent protected route checks.</action>
  <verify>go test ./services/api-gateway/... -run "SSO|SCIM|Deactivate|Refresh|Router"</verify>
  <done>Route wiring and post-deactivation denial behavior are verified in gateway test suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go test ./services/api-gateway/... -run "SSO|SCIM|Router|Refresh|Breakglass"
- [ ] Manual smoke in local env: call each new route and verify expected status code/auth behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- GOV-03 flow is reachable and enforceable through API gateway routes end-to-end

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-04-SUMMARY.md`
</output>
