---
phase: 01-identity-environment-and-connector-foundation
plan: "05"
type: execute
wave: 5
depends_on:
  - "01-02"
  - "01-04"
files_modified:
  - services/api-gateway/internal/middleware/middleware.go
  - services/api-gateway/internal/authorization/environment_policy.go
  - services/api-gateway/internal/authorization/team_policy.go
  - services/integration-service/src/main.rs
  - services/integration-service/src/middleware/mod.rs
  - services/integration-service/src/connections/api.rs
  - services/integration-service/src/storage/mod.rs
autonomous: true
gap_closure: true
requirements:
  - GOV-02
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - "Missing or invalid environment context is rejected; no default-to-dev fallback remains on protected paths"
    - "Team and environment scopes are both enforced for read/write access"
    - "Gateway and integration-service enforce the same strict context contract"
  artifacts:
    - path: "services/integration-service/src/main.rs"
      provides: "Strict tenant/environment middleware usage without optional bypass"
      contains: "tenant_context_middleware"
    - path: "services/integration-service/src/middleware/mod.rs"
      provides: "Required environment/team extraction and validation"
      contains: "x-environment"
    - path: "services/api-gateway/internal/authorization/team_policy.go"
      provides: "Team-scope authorization checks integrated with environment RBAC"
      min_lines: 20
  key_links:
    - from: "services/api-gateway/internal/middleware/middleware.go"
      to: "services/integration-service/src/middleware/mod.rs"
      via: "Validated context forwarded and revalidated downstream"
      pattern: "x-environment|x-team-id"
    - from: "services/api-gateway/internal/authorization/environment_policy.go"
      to: "services/api-gateway/internal/authorization/team_policy.go"
      via: "Combined scope decision for operation authorization"
      pattern: "Allow|Authorize"
---

<objective>
Close authorization bypasses by enforcing strict environment/team context through gateway and integration service.

Purpose: satisfy GOV-02 and PLAT-01 with no implicit defaults and no optional middleware path for protected APIs.
Output: strict context middleware contract and combined team+environment authorization checks.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-02-SUMMARY.md
@services/integration-service/src/main.rs
@services/integration-service/src/middleware/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove optional tenant/environment middleware path</name>
  <files>services/integration-service/src/main.rs, services/integration-service/src/middleware/mod.rs</files>
  <action>Replace optional context middleware usage with strict middleware for protected routes. Remove any default behavior that infers `dev` when environment is missing, and return explicit authorization errors instead.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml environment_context -- --nocapture</verify>
  <done>Protected routes fail closed when tenant/environment headers are missing or invalid.</done>
</task>

<task type="auto">
  <name>Task 2: Implement team-scope authorization path aligned with environment policy</name>
  <files>services/api-gateway/internal/authorization/environment_policy.go, services/api-gateway/internal/authorization/team_policy.go, services/api-gateway/internal/middleware/middleware.go</files>
  <action>Add/extend team-scope checks so access decisions require both environment permission and team membership/role entitlement. Ensure policy evaluation happens before forwarding protected requests downstream.</action>
  <verify>go test ./services/api-gateway/internal/authorization ./services/api-gateway/internal/middleware -run "Team|Environment|RBAC"</verify>
  <done>Requests are denied unless user satisfies both team and environment authorization constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce and test downstream fail-closed behavior for context mismatch</name>
  <files>services/integration-service/src/connections/api.rs, services/integration-service/src/storage/mod.rs, services/integration-service/src/middleware/mod.rs</files>
  <action>Add API/storage checks ensuring requested resource team/environment matches validated context. Reject cross-scope access and mismatched headers even when tenant matches.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml cross_scope_denial -- --nocapture</verify>
  <done>Cross-team and cross-environment access attempts fail consistently with explicit denial responses.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go test ./services/api-gateway/internal/authorization ./services/api-gateway/internal/middleware
- [ ] cargo test --manifest-path services/integration-service/Cargo.toml
- [ ] Negative-case manual checks: missing environment header, invalid environment, unauthorized team, cross-team resource access
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- GOV-02 and PLAT-01 are enforced with strict fail-closed context handling and team/environment scope checks

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-05-SUMMARY.md`
</output>
