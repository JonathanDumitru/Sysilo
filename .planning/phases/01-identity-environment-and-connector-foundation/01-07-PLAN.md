---
phase: 01-identity-environment-and-connector-foundation
plan: "07"
type: execute
wave: 7
depends_on:
  - "01-03"
  - "01-06"
files_modified:
  - packages/sdk/typescript/src/connector.ts
  - packages/sdk/typescript/src/index.ts
  - packages/frontend/web-app/src/services/connections.ts
  - packages/frontend/web-app/src/pages/ConnectionsPage.tsx
  - services/integration-service/src/connectors/registry.rs
  - services/integration-service/src/connectors/specs.rs
  - services/integration-service/src/connections/api.rs
autonomous: true
gap_closure: true
requirements:
  - INVT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - "SDK connector exports are the single metadata authority consumed by frontend and backend mapping paths"
    - "Frontend local connector type duplication is removed"
    - "Connector registry/spec validation remains aligned with SDK metadata contract"
  artifacts:
    - path: "packages/sdk/typescript/src/connector.ts"
      provides: "Authoritative connector metadata and exports"
      contains: "SUPPORTED_CONNECTORS"
    - path: "packages/frontend/web-app/src/services/connections.ts"
      provides: "Connector UI metadata sourced from SDK exports"
      contains: "@sysilo/sdk-typescript"
    - path: "services/integration-service/src/connectors/specs.rs"
      provides: "Backend connector spec mapping aligned to SDK metadata identifiers"
      min_lines: 25
  key_links:
    - from: "packages/sdk/typescript/src/connector.ts"
      to: "packages/frontend/web-app/src/pages/ConnectionsPage.tsx"
      via: "UI connector list and auth mode rendering use SDK metadata"
      pattern: "SUPPORTED_CONNECTORS"
    - from: "packages/sdk/typescript/src/connector.ts"
      to: "services/integration-service/src/connectors/specs.rs"
      via: "Shared connector identifiers/spec fields are contract-tested"
      pattern: "connector_id|auth_modes"
---

<objective>
Unify connector metadata authority so SDK exports define supported connectors and auth metadata across the stack.

Purpose: close PLAT-02 consistency gap caused by frontend-local connector metadata duplication.
Output: SDK-authoritative metadata flow with compatibility checks for frontend and integration-service mappings.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identity-environment-and-connector-foundation/01-VERIFICATION.md
@packages/sdk/typescript/src/connector.ts
@packages/frontend/web-app/src/services/connections.ts
@services/integration-service/src/connectors/registry.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote SDK connector metadata exports as canonical contract</name>
  <files>packages/sdk/typescript/src/connector.ts, packages/sdk/typescript/src/index.ts</files>
  <action>Stabilize and document exported connector metadata fields (identifier, label, auth modes, capability flags) and ensure they are importable by frontend consumers with typed guarantees.</action>
  <verify>pnpm --filter @sysilo/sdk-typescript test -- connector-metadata</verify>
  <done>SDK exports provide complete, typed connector metadata contract for consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Remove frontend-local metadata duplication</name>
  <files>packages/frontend/web-app/src/services/connections.ts, packages/frontend/web-app/src/pages/ConnectionsPage.tsx</files>
  <action>Replace local `CONNECTOR_TYPES` constants with SDK imports. Keep UI behavior equivalent while making metadata source single-authority and preventing drift.</action>
  <verify>pnpm --filter web-app test -- connections-metadata-source</verify>
  <done>Frontend connector rendering and validation are fully sourced from SDK exports.</done>
</task>

<task type="auto">
  <name>Task 3: Align backend registry/spec mapping and add contract drift checks</name>
  <files>services/integration-service/src/connectors/registry.rs, services/integration-service/src/connectors/specs.rs, services/integration-service/src/connections/api.rs</files>
  <action>Update backend spec mapping to use same connector identifiers and auth mode semantics as SDK exports. Add contract tests/fixtures to detect mismatch between backend registry and SDK metadata set.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml connector_spec_contract -- --nocapture</verify>
  <done>Backend connector registry/spec remains synchronized with SDK metadata authority and mismatch is test-detected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm --filter @sysilo/sdk-typescript test -- connector-metadata
- [ ] pnpm --filter web-app test -- connections-metadata-source
- [ ] cargo test --manifest-path services/integration-service/Cargo.toml connector_spec_contract -- --nocapture
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PLAT-02 is enforced through SDK-authoritative connector metadata across frontend and backend paths

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-07-SUMMARY.md`
</output>
