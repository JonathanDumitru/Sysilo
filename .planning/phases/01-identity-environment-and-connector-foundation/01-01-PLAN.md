---
phase: 01-identity-environment-and-connector-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/internal/auth/oidc.go
  - services/api-gateway/internal/handlers/auth_sso.go
  - services/api-gateway/internal/handlers/auth_breakglass.go
  - services/api-gateway/internal/handlers/auth_session.go
  - services/api-gateway/internal/auth/tokens.go
  - services/api-gateway/internal/handlers/scim_users.go
  - services/api-gateway/internal/middleware/middleware.go
  - services/api-gateway/internal/storage/users.go
  - services/api-gateway/internal/storage/migrations/001_phase1_identity.sql
autonomous: true
requirements:
  - GOV-03
user_setup:
  - service: enterprise_idp
    why: "OIDC SSO and SCIM provisioning require tenant-owned IdP setup and secrets"
    env_vars:
      - name: OIDC_ISSUER_URL
        source: "Enterprise IdP admin portal -> OIDC issuer metadata URL"
      - name: OIDC_CLIENT_ID
        source: "Enterprise IdP app registration -> client ID"
      - name: OIDC_CLIENT_SECRET
        source: "Enterprise IdP app registration -> client secret"
      - name: SCIM_BEARER_TOKEN
        source: "Enterprise IdP SCIM provisioning app -> bearer token"
    dashboard_config:
      - task: "Create OIDC application for Sysilo"
        location: "Enterprise IdP admin portal -> Applications"
        details: "Set callback URL to https://<sysilo-domain>/auth/sso/callback"
      - task: "Enable SCIM provisioning"
        location: "Enterprise IdP admin portal -> Provisioning/SCIM"
        details: "Set SCIM base URL to https://<sysilo-domain>/scim/v2"
    local_dev:
      - "Provide local callback URL override for http://localhost:<gateway-port>/auth/sso/callback"
must_haves:
  truths:
    - "User can sign in via enterprise OIDC SSO and receives a valid Sysilo session"
    - "SSO remains primary while controlled local admin break-glass recovery is available and auditable"
    - "First successful SSO login creates a local user via JIT provisioning"
    - "Sessions use short-lived access tokens with silent refresh via refresh-token rotation"
    - "SCIM deactivate blocks subsequent API access for that user"
  artifacts:
    - path: "services/api-gateway/internal/handlers/auth_sso.go"
      provides: "OIDC login/callback handlers with JIT provisioning"
      exports: ["StartSSO", "HandleSSOCallback"]
    - path: "services/api-gateway/internal/handlers/auth_breakglass.go"
      provides: "Controlled local admin break-glass login and recovery flow"
      exports: ["StartBreakglassLogin", "CompleteBreakglassLogin"]
    - path: "services/api-gateway/internal/handlers/auth_session.go"
      provides: "Silent refresh endpoint for short-lived access token renewal"
      exports: ["RefreshSession"]
    - path: "services/api-gateway/internal/handlers/scim_users.go"
      provides: "SCIM user create/update/deactivate handlers"
      contains: "Deactivate"
    - path: "services/api-gateway/internal/middleware/middleware.go"
      provides: "Request-time user active/session-version enforcement"
      contains: "session_version"
  key_links:
    - from: "services/api-gateway/internal/handlers/auth_sso.go"
      to: "services/api-gateway/internal/storage/users.go"
      via: "JIT user upsert on successful OIDC callback"
      pattern: "(Create|Upsert).*User"
    - from: "services/api-gateway/internal/handlers/scim_users.go"
      to: "services/api-gateway/internal/middleware/middleware.go"
      via: "SCIM deactivation invalidates active sessions"
      pattern: "session_version"
---

<objective>
Implement enterprise identity entry and lifecycle controls with OIDC SSO, JIT provisioning, and SCIM user lifecycle handling.

Purpose: Establish GOV-03 identity foundation required before environment authorization and connector activation controls.
Output: OIDC auth handlers, SCIM user APIs, and request-time access invalidation for deprovisioned users.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-CONTEXT.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-RESEARCH.md
@services/api-gateway/internal/middleware/middleware.go
@services/api-gateway/internal/storage/users.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement OIDC login/callback with JIT user provisioning</name>
  <files>services/api-gateway/internal/auth/oidc.go, services/api-gateway/internal/handlers/auth_sso.go, services/api-gateway/internal/storage/users.go</files>
  <action>Add OIDC discovery, auth redirect, callback token exchange, and issuer/audience validation. On successful callback, map tenant and IdP subject, create or update user record for first login (JIT), then issue Sysilo JWT with user status and session-version claims.</action>
  <verify>go test ./services/api-gateway/internal/auth ./services/api-gateway/internal/handlers -run "SSO|OIDC|JIT"</verify>
  <done>SSO flow is fully implemented and first login creates local user when tenant/domain policy passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add controlled local admin break-glass recovery path</name>
  <files>services/api-gateway/internal/handlers/auth_breakglass.go, services/api-gateway/internal/storage/users.go, services/api-gateway/internal/storage/migrations/001_phase1_identity.sql</files>
  <action>Implement a local admin-only recovery login flow gated by explicit break-glass eligibility checks, strong credential verification, and mandatory audit/event recording. Keep this path isolated from regular user auth and enforce that non-admin users cannot authenticate through it.</action>
  <verify>go test ./services/api-gateway/internal/handlers ./services/api-gateway/internal/storage -run "Breakglass|Recovery|AdminOnly"</verify>
  <done>Break-glass login works only for approved local admin recovery accounts and all usage is auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Add SCIM Users endpoints with deactivation semantics</name>
  <files>services/api-gateway/internal/handlers/scim_users.go, services/api-gateway/internal/storage/users.go, services/api-gateway/internal/storage/migrations/001_phase1_identity.sql</files>
  <action>Implement SCIM v2 Users create/update/deactivate operations mapped to local user records. Persist active/inactive state and increment session-version on deactivate to invalidate existing sessions rapidly.</action>
  <verify>go test ./services/api-gateway/internal/handlers -run "SCIM|Provision|Deactivate"</verify>
  <done>SCIM requests can provision and deactivate users, and deactivated users are marked inactive with rotated session-version.</done>
</task>

<task type="auto">
  <name>Task 4: Enforce short-lived access tokens with silent refresh and request-time session checks</name>
  <files>services/api-gateway/internal/auth/tokens.go, services/api-gateway/internal/handlers/auth_session.go, services/api-gateway/internal/middleware/middleware.go, services/api-gateway/internal/storage/users.go, services/api-gateway/internal/storage/migrations/001_phase1_identity.sql</files>
  <action>Issue short-lived access JWTs and longer-lived refresh tokens with rotation and revocation tracking. Add silent refresh endpoint that exchanges valid refresh tokens for new access tokens without interactive re-login. Update middleware to verify user is active and token session-version equals persisted value before allowing route execution.</action>
  <verify>go test ./services/api-gateway/internal/auth ./services/api-gateway/internal/handlers ./services/api-gateway/internal/middleware -run "TokenTTL|Refresh|SilentRefresh|Session|Inactive|Auth"</verify>
  <done>Clients keep sessions alive through silent refresh while stale, revoked, or inactive-user tokens are rejected on every request.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go test ./services/api-gateway/...
- [ ] SSO callback test validates JIT provisioning path
- [ ] Break-glass test confirms admin-only recovery path and audit event emission
- [ ] Token test confirms short access-token TTL and silent refresh rotation behavior
- [ ] SCIM deactivate test confirms immediate API denial on next request
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- GOV-03 is satisfied by OIDC SSO + SCIM lifecycle enforcement

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-01-SUMMARY.md`
</output>
