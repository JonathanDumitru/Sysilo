---
phase: 01-identity-environment-and-connector-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - packages/sdk/typescript/src/connector.ts
  - services/integration-service/src/connectors/registry.rs
  - services/integration-service/src/connections/mod.rs
  - services/integration-service/src/connections/api.rs
  - packages/frontend/web-app/src/services/connections.ts
  - packages/frontend/web-app/src/hooks/useConnections.ts
  - packages/frontend/web-app/src/pages/ConnectionsPage.tsx
autonomous: true
requirements:
  - INVT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - "User can create a connector as draft, run a real connectivity test, then activate when test passes"
    - "Failed connectivity tests block connector activation"
    - "Credential values are never returned in plaintext after save or update"
  artifacts:
    - path: "services/integration-service/src/connections/mod.rs"
      provides: "Connection lifecycle state machine and secure credential handling"
      contains: "draft"
    - path: "services/integration-service/src/connectors/registry.rs"
      provides: "Prioritized connector implementations registered for health checks"
      min_lines: 40
    - path: "packages/frontend/web-app/src/pages/ConnectionsPage.tsx"
      provides: "Auth-type-aware onboarding flow with test-before-activate gating"
      contains: "test"
  key_links:
    - from: "services/integration-service/src/connections/api.rs"
      to: "services/integration-service/src/connectors/registry.rs"
      via: "test endpoint dispatches health check through connector registry"
      pattern: "health(check|_check)"
    - from: "packages/frontend/web-app/src/pages/ConnectionsPage.tsx"
      to: "services/integration-service/src/connections/api.rs"
      via: "UI blocks activation until test status is successful"
      pattern: "activate"
---

<objective>
Harden connector onboarding and runtime so prioritized enterprise connectors use stable SDK contracts, secure secret handling, and test-before-activate lifecycle gates.

Purpose: Deliver INVT-01 and PLAT-02 with reliable connector management and secure credential behavior.
Output: Connector registry-backed health checks, lifecycle state transitions, and onboarding UX guardrails.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-CONTEXT.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-RESEARCH.md
@.planning/phases/01-identity-environment-and-connector-foundation/01-02-SUMMARY.md
@packages/sdk/typescript/src/connector.ts
@services/integration-service/src/connections/mod.rs
@packages/frontend/web-app/src/pages/ConnectionsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement connector lifecycle state machine with registry-backed health tests</name>
  <files>services/integration-service/src/connectors/registry.rs, services/integration-service/src/connections/mod.rs, services/integration-service/src/connections/api.rs</files>
  <action>Define draft/tested/active/error lifecycle transitions and enforce legal transitions server-side. Wire `/connections/:id/test` to connector-specific health check implementations through a shared registry contract for prioritized connectors.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml connector_lifecycle -- --nocapture</verify>
  <done>Connections cannot become active unless latest test succeeded, and test endpoint uses real connector health checks.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce write-only and replace-only credential semantics</name>
  <files>services/integration-service/src/connections/mod.rs, services/integration-service/src/connections/api.rs, packages/frontend/web-app/src/services/connections.ts</files>
  <action>Ensure API stores encrypted credentials, never returns plaintext secrets, and only supports credential replacement (not readback). Return masked indicators such as `has_credentials` for edit flows.</action>
  <verify>cargo test --manifest-path services/integration-service/Cargo.toml secret_handling -- --nocapture</verify>
  <done>Credential responses are masked only, with no plaintext fields in any read or update response.</done>
</task>

<task type="auto">
  <name>Task 3: Update onboarding UI for auth-type-specific flow and activation gating</name>
  <files>packages/frontend/web-app/src/hooks/useConnections.ts, packages/frontend/web-app/src/pages/ConnectionsPage.tsx, packages/frontend/web-app/src/services/connections.ts, packages/sdk/typescript/src/connector.ts</files>
  <action>Implement guided wizard behavior per auth mode (credential/oauth/api_key), support draft saves, surface test status, and block activate action until successful test. Keep connector SDK types authoritative for supported connector metadata and onboarding requirements.</action>
  <verify>pnpm --filter web-app test -- connections-onboarding && pnpm --filter @sysilo/sdk-typescript test</verify>
  <done>Users can complete auth-type-aware onboarding, run connection tests, and only activate after passing tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path services/integration-service/Cargo.toml
- [ ] pnpm --filter web-app test -- connections-onboarding
- [ ] pnpm --filter @sysilo/sdk-typescript test
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- INVT-01 and PLAT-02 are satisfied with secure credentials, tested activation, and SDK-backed connector management

</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-environment-and-connector-foundation/01-03-SUMMARY.md`
</output>
